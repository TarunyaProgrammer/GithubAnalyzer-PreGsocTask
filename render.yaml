services:
  # NestJS Backend
  - type: web
    name: webiu-backend
    env: node
    plan: free # Can be upgraded to 'starter' for more performance
    buildCommand: cd backend && npm install && npx prisma generate && npm run build
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: webiu-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: webiu-redis
          property: connectionString
      - key: PORT
        value: 3000
      - key: GITHUB_TOKEN
        sync: false # Set manually in Render dashboard
      - key: GITHUB_ORG
        sync: false # Set manually in Render dashboard
      - key: GITHUB_WEBHOOK_SECRET
        sync: false # Set manually in Render dashboard

databases:
  - name: webiu-db
    plan: free # Render Free tier Postgres
    databaseName: webiu
    user: webiu

redis:
  - name: webiu-redis
    plan: free # Render Free tier Redis (shared)
    ipAllowList: [] # Only allow internal connections
